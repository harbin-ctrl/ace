#ifndef ACE_AROS_REAL_SYSTEM_H
#define ACE_AROS_REAL_SYSTEM_H

/* The checked-out AROS tree generates this header as part of its platform
   build.  Reuse its host-neutral definition for the compile-only target. */
#include <system.h>

#endif
