#ifndef AMIGA_SHELL_LIBRARIES_DOS_H
#define AMIGA_SHELL_LIBRARIES_DOS_H

/* Pre-2.0 AmigaOS header name for what is now <dos/dos.h>. Real AROS keeps
   this as a one-line redirect for old source; so does ACE. */
#include <dos/dos.h>

#endif
