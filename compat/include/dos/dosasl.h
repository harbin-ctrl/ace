#ifndef AMIGA_SHELL_DOS_DOSASL_H
#define AMIGA_SHELL_DOS_DOSASL_H

/* AChain/AnchorPath and the pattern-token constants live in dos/dos.h in
   this compat tree rather than split out the way real AROS does; this file
   exists only so #include <dos/dosasl.h> in real AROS source resolves. */
#include <dos/dos.h>

#endif
