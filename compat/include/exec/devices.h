#ifndef AMIGA_SHELL_EXEC_DEVICES_H
#define AMIGA_SHELL_EXEC_DEVICES_H

/* Dir.c includes this but uses no symbol from it: it never opens a device
   directly, only Lock()/Examine()/ExNext() through dos.library. */

#endif
