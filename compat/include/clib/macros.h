#ifndef AMIGA_SHELL_CLIB_MACROS_H
#define AMIGA_SHELL_CLIB_MACROS_H

#define MIN(x, y) ((x) < (y) ? (x) : (y))
#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define ABS(x) (((x) < 0) ? -(x) : (x))

#endif
